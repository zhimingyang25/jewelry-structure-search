# design.md — 珠宝同款比对检索引擎（第一版）设计 v2

需求基线：`requirements.md`（唯一来源）。实施说明：`IMPLEMENT-PROMPT.md`。评审意见与逐条回应：`design-review.md`。
本文只写方案与技术拍板；未写入 `requirements.md` 第 3～10 节的功能一律不做。

v2 相对 v1 的主要变化：显卡确认为 P104-100 后，索引成本不再是瓶颈，因此改为高分辨率、多变体索引、库图也裁剪、全库 CLIP 复核品类；主键改为内容哈希；入口减为两个 bat；加离线评测工具；核对结论改三档。

---

## 0. Context

**问题。** 现有 `jewelry-structure-search` 用视觉大模型给每张图填结构标签，再按标签词重合打分（`src/server.mjs:67` `searchByTags`）。这只能找到「同类」，找不到「那一款」。`requirements.md` 第 5 节已确认：标签对词不能再当找回同款的主搜索。

**目标。** 客人给一张成品精修图，在约数万张犀牛/JCAD 渲染图里找出同款或半天能改的款。同款的判定是**结构**，不是颜色，不是标签词。验收门槛：A1（铁定同款进前 20）、A4（品类隔离）、A6（无同款先说明）、A10（打开文件夹）。

**最难的点。** 客人精修图/实拍图 vs 犀牛渲染图是跨域比对。第一版没有珠宝同款训练数据，只能靠通用结构特征模型加多套预处理变体，用离线评测挑最好的一套。A1 是效果型目标，本设计保证机制与可切换性，不保证结果。

**保留的壳。** 图库扫描（`src/scanner.mjs`）、`data/images.json` 路径清单、打开文件夹（`explorer.exe /select`）、Node 静态网页与端口逻辑。检索内核换掉。

---

## 1. 现状盘点

| 项 | 现状 |
|---|---|
| 图库 | `data/images.json`：约数万张，分布在多个本地目录。`id` 每次合并扫描后重排（`mergeCatalogs`），不能作主键。 |
| 标签库 | `data/structure-tags.json`：已有一批结构标签，覆盖了大部分已扫描图片；仍有部分图片暂无标签。旧标签质量未验证，只能当参考信号之一，不能单独决定品类。 |
| 视觉 API | `src/openai-client.mjs`：OpenAI 兼容 chat/completions，走 `.env` 里的代理，模型 `gpt-5.4-mini`。复用做查询图的**品类 + 主体框**。 |
| 机器 | Windows 10；`python` 默认指向 3.14.5，另有 3.11/3.12 可用（`py -3.11`），均无 torch；显卡 **NVIDIA P104-100**（Pascal GP104，算力 6.1），**8192 MiB 显存**，驱动 **580.88**（支持 CUDA 13.0 运行时，向下兼容 cu118/cu126），FP16 无加速。桌面应用与 Rhino 常驻占约 1.9 GB 显存，可用约 6 GB。 |
| 数据保护 | `images.json`、`structure-tags.json` 只读。索引放新目录 `data/index/`。 |

---

## 2. 总体架构

```
浏览器 web/           Node src/server.mjs (8787)                Python search_service/ (127.0.0.1:8788)
─────────────        ──────────────────────────────           ─────────────────────────────────────
上传客人图 ─POST─▶  /api/search
                      1. GPT 视觉：品类 + 主体框（超时 12s）
                      2. POST /search {query_id, image, box, category}
                                                            ▶ 框缺失 → CLS 注意力前景框 → 整图
                                                            ▶ 原图上裁剪 → 缩到 448
                                                            ▶ 8 视角 TTA → DINOv2 向量
                                                            ▶ 品类过滤（多标签）
                                                            ▶ 对彩色/灰度、整图/裁剪多路索引取分并融合
                                                            ▶ 可选：前 200 做 patch 级局部重排
                                                            ▶ 完全重复文件合并；取前 100
                                                            ▶ 无同款判定（绝对阈值 或 差距阈值）
                      ◀ 结果 JSON
列表 + 状态行 ◀───
点击卡片 ─POST─▶  /api/open-folder（先检查文件存在）
「开始认图」─POST─▶ /api/index/start ─▶ Python 后台建索引，/api/status 轮询进度，完成后热加载
```

- **Node 是唯一入口。** `npm run start` 时 Node spawn Python 服务，退出时一起杀掉。用户只有 `安装.bat`（一次）和 `启动.bat`（每天）。
- **Python 常驻**，模型只加载一次。只监听 127.0.0.1。查询图只在内存，不落盘。
- **建索引**由网页按钮触发，在 Python 服务内后台跑，可中断续跑，进度显示在页面状态行。

---

## 3. 关键技术选型与拍板

### 3.1 相似度模型：DINOv2（主排序）+ 变体矩阵

- **选 DINOv2 的理由**：自监督、偏结构与局部几何，实例级检索优于 CLIP；CLIP 偏语义，会把「都是光环戒指」排一起，正是标签法的毛病。
- **默认**：`facebook/dinov2-base`，输入 **448×448**（patch 14 → 32×32 token），CLS + 平均 patch 拼接后 L2 归一化。裁剪在原图分辨率上做再缩放，小主体不丢细节。
- **变体矩阵**（GPU 下每套约 30～60 分钟，都建出来供评测切换）：

| 维度 | 取值 |
|---|---|
| 输入尺寸 | 224 / 336 / **448** |
| 颜色 | **彩色 + 灰度双路融合** / 仅彩色 / 仅灰度 |
| 库图视图 | **整图 + 注意力裁剪双向量取最大** / 仅整图 |
| 模型 | **base** / large（8GB 显存可跑） |
| 二阶段 | 关 / **前 200 做 patch 级局部重排**（拉开爪数、圈数等细节） |
| 语义融合 | **关** / CLIP 分数 0.2 权重（惩罚「头完全不同」，见 A7） |

- 粗体为初始默认；最终默认由 §10 的评测决定，不用验收对子调参。
- 约数万张 × 4 路 × 768 维 fp32，当前规模下可放进内存做暴力余弦；到百万张量级才需要 FAISS。

### 3.2 运行时：PyTorch（Pascal 兼容版）+ transformers

- P104-100 是 sm_61。新版 cu128 轮子已去掉 Pascal，**固定装 torch 2.7.x 的 cu126 轮子**（驱动 580.88 向下兼容），安装自检用 `torch.cuda.get_arch_list()` 确认含 `sm_61`，不含则回退 cu118 轮子。推理用 FP32（Pascal FP16 无加速）。
- venv 必须用 `py -3.11` 创建：系统默认 `python` 是 3.14，torch 轮子未必有。
- `安装.bat` 两步走：① 建 `.venv`（Python 3.11），装 CPU 版 torch + 依赖，自检通过 → 至少能用；② 尝试换装 CUDA 版，自检 `torch.cuda.is_available()`，失败则回滚保留 CPU 版。每步中文报告。
- 模型下载：Hugging Face，可选 `HF_ENDPOINT=https://hf-mirror.com`；另提供离线包放 `models/` 目录，安装脚本下载失败时明确提示用离线包。
- 内存与显存：服务常驻约 3～4 GB 内存、2～3 GB 显存；机器最低 16 GB 内存。

### 3.3 品类：查询图 GPT 为主，库图全库 CLIP 复核，多标签防丢

- **查询图**：GPT 精简 prompt，只要 `{category, main_item_box}`。GPT 失败或 unclear → CLIP 零样本；若 CLIP 置信也低（top1 < 0.5 或与第二名差距 < 0.15）→ **不自动搜**，界面提示「没看出品类，请选一个」。不混品类，也不装确定。
- **库图**：建索引时对已扫描图库全部跑 CLIP ViT-B/32 零样本（GPU 几分钟）。旧标签与 CLIP 一致 → 取之；不一致或无旧标签 → **两个品类都挂上**（多标签），任一品类搜索都能命中。旧标签不再单独决定任何一张图的归属。
- **品类组**（`config.json` 可改）：`ring`、`pendant/necklace`（含 charm）、`earring`、`bracelet`、`bangle`、`brooch`、`other`。吊坠与项链合并：同一款吊坠渲染图两种标法都常见，属同一物件；界面诚实标「吊坠/项链」。**验收时若用户不认可，配置拆开。** 手链与手镯外形差异明显，拆开。
- **硬过滤**：结果只含所选品类组，不足 100 张不补。

### 3.4 主体裁剪（需求 3.6）

- 顺序：GPT 框（外扩 12%）→ 缺失/非法（面积 < 5% 或 > 95%）时用 DINOv2 CLS 注意力图算前景框 → 仍失败用整图。
- **库图同样做注意力裁剪**，每张存整图与裁剪两个向量，查询与两者比较取最大。库里的拼图、带手模、装饰背景由此缓解。
- 不做框选、不拆每一件。

### 3.5 颜色（需求 3.2、A8）

- 彩色索引与灰度索引都建，默认两路相似度平均。灰度削弱成色/石色，彩色保留主石边界与材质层次，融合取两者之长。任一路可关，由评测定。

### 3.6 查询增强

- 查询图 8 视角 TTA（0/90/180/270 度 × 水平翻转），向量平均。吊坠倒置、耳环侧放、斜拍由此缓解。库图不做。

### 3.7 「没找到很像的同款」判定（需求 12.4 留给实现定）

- 触发条件（任一）：① top1 < `absThreshold[品类]`；② top1 − median(第 20～100 名) < `gapThreshold`。差距条件在不同品类/风格下比单一绝对值稳。
- 校准分两层：`calibrate.py` 用库内扰动只定「近似重复」上界；跨域阈值必须来自用户真实对子与「确认无同款」的图。拿到之前取保守值，**宁可多提示「没找到」，不把有点像装成同款**。
- 验收对子是测试集，不参与校准。

### 3.8 结果列表（需求 3.4、A9）

- 相似度降序，最多 `topK`（默认 100）。
- **不按款分组、不合并多角度**。只合并内容哈希完全相同的重复文件（同一文件不同位置），这不是分组。近似重复挤占前 100 记为已知局限。
- 卡片显示缩略图、文件名、文件夹、淡色「相似度 0.73」、「打开所在位置」。需求禁止的是同款/类似标签，不是数值。
- 列表顶部常驻一行：低于阈值显示「没找到很像的同款，下面是类似、可改的款」；否则只显示「最高相似度 0.81」，不写「找到同款」。另固定一句「系统不判断同款，请自行看图」。

### 3.9 草图（需求 3.1、A14）

- 与成品图同流程，无特殊分支。灰度路径对线稿有利。只保证有结果不报错；用户若提供草图样本则进评测观察，不为它单独优化。

### 3.10 已知局限（第一版不解决）

- 「半天能改」是人的工作量判断，模型不估；只按像的程度排序。
- 「完全另一种头不要排太前」（A7）无硬规则，靠模型聚集与可选 CLIP 语义融合，效果待验。
- 模型文件是否与图片同文件夹属图库整理问题，系统只负责打开图片所在位置。

---

## 4. 索引设计

目录 `data/index/<signature>/`：

| 文件 | 内容 |
|---|---|
| `vectors_<variant>.npy` | float32 `[N, D]`，L2 归一化；variant ∈ {color_full, color_crop, gray_full, gray_crop} |
| `meta.json` | `{signature, model, input_size, preprocess_version, built_at, items:[{sha1, path, size, mtime_ms}]}`，行号与所有 `.npy` 对齐 |
| `categories.json` | `{clip_signature, items:{sha1:{tag, clip, clip_prob, groups:[...]}}}`，与向量分开、各自签名 |
| `failed.json` | 打不开的图：路径 + 原因 |

- **主键 = 文件内容 sha1**；path/size/mtime 是属性。移动、改盘符、整理目录后向量复用，只更新路径；hash 消失才删行。sha1 在建索引读文件时顺带算，不额外遍历。
- **signature** = hash(模型, 输入尺寸, 颜色模式, 裁剪方式, 预处理版本)。不一致拒绝加载并提示重建。阈值、品类分组、CLIP prompt 不影响向量，不触发重建。
- **写入**：每 500 张把向量与 meta 成对写到 `index.tmp/`，整目录原子改名替换。没有单独进度日志；续跑时不在 meta 里的一律重算。
- **增量**：读 `images.json`，按 sha1 对照 meta，只算新图；删除不存在的。
- 服务把 meta 与 `images.json` 按 path 对上得 `sha1 → 当前 id`，前端 `/asset/:id`、`/api/open-folder` 不变。
- **检索范围 = `images.json` 已扫描图库**。不扫描其它路径（需求 12.1 未定）。

---

## 5. 接口设计

### Python 服务（127.0.0.1:8788，端口固定，占用即启动失败并中文报错）

| 方法 路径 | 入参 | 出参 |
|---|---|---|
| `GET /health` | – | `{ok, device, model, signature, indexed, total, failed, indexing:{running, done, total}}` |
| `POST /search` | `{query_id, image_b64?, box?, category?, limit?}` | `{query_id, category_used, results:[{sha1, path, score}], no_close_match, top_score, elapsed_ms}`；`image_b64` 缺省时用 query_id 缓存 |
| `POST /classify` | `{image_b64}` | `{category, prob, margin}` |
| `POST /index/start` | `{rebuild?:bool}` | 后台开始建索引 |
| `POST /index/stop` | – | 停止，已写部分保留 |
| `POST /reload-index` | – | 热加载 |

查询缓存：`query_id` = 图片 sha1；服务端内存保存最近 5 个查询的图、框、向量，30 分钟过期；请求体上限 20 MB。

### Node（面向网页）

| 方法 路径 | 说明 |
|---|---|
| `POST /api/search` | 入 `{imageDataUrl?, query_id?, category?}`。流程：无 `category` → 调 GPT（12 秒超时）得品类+框 → 调 Python `/search`。有 `category`（用户手动） → 复用 query_id 缓存的框，缓存丢失自动重调 GPT 只取框。返回 `{query_id, detected_category, category_source, category_uncertain, category_used, no_close_match, top_score, results:[{...image字段, score}], elapsed}`。 |
| `GET /api/status` | 现有字段 + `index:{...}` + `services:{python, model, gpt}` 三者健康。 |
| `POST /api/index/start` / `stop` | 转发 Python。 |
| `POST /api/open-folder` | 先 `fs.existsSync`，不存在返回「文件已不在原位置」。 |
| `POST /api/scan` | 不变。 |
| `/api/analyze-and-search`、`/api/search-tags` | **删除**，连同手动标签界面。只留一套搜索逻辑。 |

---

## 6. 网页改动

`web/index.html` 小改，`web/app.js` 重写状态逻辑，样式沿用。

1. **左栏**：上传；「找同款」按钮；品类下拉（自动 / 戒指 / 吊坠项链 / 耳环 / 手链 / 手镯 / 胸针 / 其它），搜索后显示「系统认为是：戒指」，改下拉后点「按这个品类重搜」（带 query_id，不重传）。新上传时下拉重置为「自动」。
2. **删掉**手动结构搜索整块。
3. **结果顶部状态行**：见 §3.8。
4. **卡片**：缩略图、文件名、文件夹、淡色相似度、「打开所在位置」。
5. **顶部状态行**：「已扫描 N，已认 N，待认 0，失败 3（查看）｜dinov2-base@448，GPU」。待认 > 0 时显示「开始认图」按钮；认图中显示进度并可停止；认完自动热加载、待认归零。
6. **故障提示**（中文，各一句 + 下一步）：Python 未启动 / 模型未下载 / 索引签名不匹配需重建 / GPT 不可用（可手动选品类继续）/ 图片打不开请另存为 JPG。索引未就绪时禁用「找同款」。

---

## 7. 文件改动清单

**新增**

```
search_service/
  __init__.py
  config.py          读 config.json 的 searchService 段
  embedder.py        DINOv2 加载、原图裁剪、448 缩放、灰度、8 视角 TTA、注意力前景框、批量向量
  category.py        品类组表、CLIP 零样本、多标签合并规则
  index_store.py     多路 .npy + meta/categories 读写、sha1 主键、签名、原子目录替换、增量
  indexer.py         后台建索引任务（可停可续，进度）
  rerank.py          可选 patch 级局部重排
  server.py          stdlib http.server(Threading)：§5 接口
  bench.py           先跑 200 张外推全库耗时
  calibrate.py       库内扰动校准（只定上界）
  eval.py            输入 eval/pairs.json（客人图路径 → 库内目标路径），输出每组名次与各变体对比
  smoke.py           随机 20 张库图查自己，自身排第一即通过；邻居只报告不判定
  check_in_library.py  按文件名查某张图是否在 images.json（验收前用）
  requirements.txt
安装.bat             两步安装 + 自检 + 预下载模型（或提示离线包）
启动.bat             npm run start（Node 拉起 Python）
eval/                验收对子与后续积累的对子（用户放）
models/              离线模型包位置（可选）
design.md, design-review.md
src/category-client.mjs   GPT 品类 + 主体框
```

**修改**

```
src/server.mjs      spawn/kill Python；/api/search、/api/status、/api/index/*；open-folder 存在性检查；删旧接口
src/openai-client.mjs   抽出 chatWithImage(prompt, imageUrl)
web/index.html, web/app.js, web/styles.css
config.json         searchService: {port:8788, python:".venv/Scripts/python.exe", embedModel, inputSize:448, variants:{...}, absThreshold:{...}, gapThreshold, topK:100, categoryGroups:{...}}
.env.example        HF_ENDPOINT
run.bat             改名为 启动.bat（保留 run.bat 作兼容）
```

**不动**：`data/images.json`、`data/structure-tags.json`、`src/scanner.mjs`、`src/scan.mjs`、`src/tag-*.mjs`、`src/taxonomy.mjs`。

---

## 8. 性能预估（P104-100，待 bench 实测修正）

| 阶段 | GPU | CPU 回退（未实测） |
|---|---|---|
| 一套变体（base@448，整图+裁剪）约数万张 | 约 40～80 分钟 | 可能 10～20 小时 |
| 全部默认变体（彩色+灰度） | 约 1.5～3 小时 | 不建议 |
| CLIP 全库品类复核 | 约 5 分钟 | 约 1 小时 |
| 单次查询 | GPT 3～8 秒 + 向量/检索 < 1 秒 | + 3～5 秒 |
| 冷启动 | 5～10 秒 | 10～15 秒 |

实施第一步先跑 `bench.py`，200 张外推后再定变体数量。

---

## 9. 风险与备选

| 风险 | 应对 |
|---|---|
| A1 不达标 | 变体矩阵已预建，`eval.py` 逐套跑，切最好的；仍不行 → 第二版用积累对子微调。 |
| A7 另一种头排前 | 开 CLIP 语义融合变体；无硬保证。 |
| Pascal 装不上 CUDA torch | 固定 torch 2.7.x+cu126，自检 `sm_61`；失败回退 cu118，再失败保留 CPU 版，索引改过夜。 |
| 显存被 Rhino 等占用 | 可用约 6 GB；base@448 batch 16 约需 2～3 GB，够用；large 变体 batch 调小。 |
| 模型下载失败 | 镜像 + 离线包。 |
| 旧标签错 | 全库 CLIP 复核 + 多标签，不再单靠旧标签。 |
| GPT 框错 | 注意力框兜底；库图也裁剪。 |
| 验收对子不在库里 | `check_in_library.py` 先查，不在库的不计入 A1。 |
| 文件已移动 | sha1 复用向量；打开前检查存在。 |

---

## 10. 验证方案

1. `bench.py`：200 张外推耗时，决定变体数量。
2. `smoke.py`：随机 20 张库图查自己，自身第一为通过。
3. `eval.py`：用户验收前提供的对子（哪怕 1～2 组）+ 确认无同款的图 → 各变体名次对比 → 定默认变体与阈值。**验收当天的 3～5 组只当测试集。**
4. 端到端：启动 → 上传库内戒指图 → 第 1 张是自己、全是戒指 → 打开文件夹选中 → 改品类「吊坠」重搜 → 只含吊坠且顶部出现「没找到很像的同款」。
5. 满图、草图、GPT 断网三种异常路径各走一次，界面提示为中文。
6. 增量：丢 3 张新图 → 扫描 → 「开始认图」只算 3 张 → 自动热加载后可搜。
7. 上线后把新对子持续放进 `eval/`，定期重跑。

---

## 11. 用户使用步骤（大白话）

**第一次**：双击 `安装.bat`，看到「安装完成」再关。它会先装能用的版本，再试显卡加速，两步都会说成功还是失败。

**每天**：双击 `启动.bat`，打开网址。第一次进去先点「开始认图」，可以开着过夜，进度在页面上；中途关了下次接着认。认完后把客人图拖进去点「找同款」，等十几秒。顶上一行会告诉你最高相似度，或者「没找到很像的同款」。点「打开所在位置」跳到那张图。品类看错了就在下拉里改，点「按这个品类重搜」。

**补了新图**：点「扫描图库」，页面会显示「待认 N 张」，再点「开始认图」。

---

## 12. 交付前还缺什么

- ~~`nvidia-smi` 输出~~ 已确认：驱动 580.88、8 GB 显存，torch 选 2.7.x+cu126。
- 用户验收前能给的任何对子（1～2 组即可）→ 定默认变体与阈值。
- 全库索引建完，`/api/status` 显示待认 0。
- 吊坠/项链是否合并，验收时用户拍板。

---

## 13. 对照 requirements.md 逐项核对（三档：机制到位 / 效果待验 / 有开关待定）

| 需求条目 | 对应 | 状态 |
|---|---|---|
| 3.1 成品图搜库 | §2、§5 | 机制到位 |
| 3.1 草图能搜不报错 | §3.9 | 机制到位；命中率效果待验 |
| 3.2 同款 = 结构一致 | §3.1 | **效果待验** |
| 3.2 颜色不拆款 | §3.5 双路融合 | 有开关待定 |
| 3.3 类似大方向对、另一种头不靠前 | §3.1 语义融合变体、§3.10 | **效果待验** |
| 3.4 逐张降序、≤100、不分组、不标注、打开文件夹 | §3.8、§5 | 机制到位 |
| 3.5 全库搜、自动品类、只出该品类、可手动纠正 | §3.3、§6.1 | 机制到位；吊坠/项链合并待用户拍板 |
| 3.6 满图取最大一件 | §3.4 | 机制到位；效果待验（A12） |
| 3.7 无同款先说明 | §3.7、§3.8 | 机制到位；阈值效果待验（A6） |
| 3.8 查询十几秒、首认可过夜、补图再认 | §8、§4、§6.5 | 机制到位；耗时待 bench |
| 3.9 允许上网 AI | 品类用 GPT | 机制到位 |
| 3.10 范围 = 当前已扫描图库 | §4 | 机制到位；验收对子须先查在库 |
| 3.11 界面可改 | §6 | 机制到位 |
| 4.1～4.8 | 同上 | 同上 |
| 5 不做清单 9 项 | §2、§3.4、§3.8、§3.10 | 全部未做（完全重复文件合并不算分组） |
| 8 异常 6 项 | §3.3、§3.4、§3.7、§3.9、§6.6、§4 | 机制到位 |
| A1 | §3、§10.3 | **效果待验** |
| A2、A3、A9、A10、A11、A13、A15 | §3.8、§5、§4 | 机制到位 |
| A4、A5 | §3.3 硬过滤 + 多标签 | 机制到位 |
| A6、A7、A8、A12 | §3.7、§3.10、§3.5、§3.4 | **效果待验** |
| A14 | §3.9 | 机制到位 |
| 12.4 阈值由实现定 | §3.7 | 有开关待定 |

**核对结论**：所有条目都有对应机制；A1、A6、A7、A8、A12 与 3.2/3.3 是效果型目标，本设计只能保证可评测、可切换，不能用架构对应表判定通过。
