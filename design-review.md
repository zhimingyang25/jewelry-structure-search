# design-review.md — 对 design.md v1 评审意见的逐条回应

日期：2026-09-16。评审共 55 条。结论分三类：接受并修改（已写入 design.md v2）、反驳、需要补充信息。

| # | 结论 | 说明 |
|---|---|---|
| 1 | 接受并修改 | 加 `eval.py` 与变体矩阵；请用户验收前先给 1～2 组对子。 |
| 2 | 部分反驳 | 无符号规则（需检测器，超范围）。加 patch 级局部重排作开关。 |
| 3 | 接受并修改 | 默认 448；224/336/448 进变体矩阵；原图上裁再缩放。 |
| 4 | 接受并修改 | 彩色+灰度双索引融合，各可关，评测定。 |
| 5 | 接受并修改 | 绝对阈值 或 差距阈值，按品类校准。 |
| 6 | 接受并修改 | 库内扰动只定上界；跨域阈值来自真实对子；先取保守值。 |
| 7 | 接受并修改 | 全库 CLIP 复核；旧标签与 CLIP 不一致时多标签。 |
| 8 | 接受 | 由 7 解决。 |
| 9 | 反驳，保留开关 | 吊坠/项链是同一物件两种标法，非需求所指的混品类。验收时用户拍板，配置可拆。 |
| 10 | 接受并修改 | 手链与手镯拆开。 |
| 11 | 接受并修改 | GPT 框 → 注意力前景框 → 整图。 |
| 12 | 接受并修改 | query_id 缓存框与向量 30 分钟；丢失自动重调 GPT。 |
| 13 | 接受并修改 | 改顺序调用：GPT（12 秒超时）→ Python 一次 /search。 |
| 14 | 接受并修改 | CLIP 也拿不准时不自动搜，请用户选品类。 |
| 15 | 接受并修改 | 卡片淡色显示相似度；顶部显示最高相似度。 |
| 16 | 反驳 | 「半天能改」是人工判断，模型不估；需求只要按像排序。记为局限。 |
| 17 | 部分接受 | CLIP 语义融合 0.2 作变体；无硬保证；效果待验。 |
| 18 | 反驳 | 需求原话「可以连续出现」「不要求分组」；强制挨着即分组。 |
| 19 | 接受并修改 | 库图也做注意力裁剪，双向量取最大。 |
| 20 | 接受并修改 | 主键改内容 sha1。 |
| 21 | 接受 | 由 20 解决：移动不重算。 |
| 22 | 接受并修改 | 两步安装，先 CPU 保底再试 CUDA，每步自检中文报告。 |
| 23 | 接受并修改 | 镜像 + 离线包。 |
| 24 | 已补充，接受并修改 | `nvidia-smi`：驱动 580.88、CUDA 13.0、8192 MiB。P104-100 为 Pascal sm_61，固定装 torch 2.7.x+cu126，自检 `sm_61`，失败回退 cu118。venv 用 `py -3.11`（默认 python 是 3.14）。 |
| 25 | 接受并修改 | 主路径改 GPU 估算；CPU 标「未实测」；加 bench.py。 |
| 26 | 接受并修改 | 写明约 3～4 GB 内存、2～3 GB 显存、最低 16 GB。 |
| 27 | 反驳 | 10 万张暴力余弦仍是几十毫秒；百万级才需 FAISS。 |
| 28 | 接受并修改 | 加 check_in_library.py；不在库的对子不计入 A1。 |
| 29 | 接受并修改 | 打开前检查存在；不存在中文提示。 |
| 30 | 接受并修改 | 状态行显示已扫描/已认/待认/失败，并校验签名。 |
| 31 | 接受并修改 | 库图失败入 failed.json 计数；查询图失败中文提示；PIL 统一转 RGB 处理透明/CMYK。 |
| 32 | 接受 | 前端 1600px JPG，Node/Python 各 20 MB 上限。 |
| 33 | 反驳，纳入 eval | 需求 A14 只要能搜不报错；有草图样本则观察。 |
| 34 | 接受 | 由 7 解决。 |
| 35 | 接受并修改 | 删除旧接口与手动标签界面。 |
| 36 | 接受并修改 | Python 端口固定，占用即失败报错。 |
| 37 | 接受并修改 | 去掉 progress.jsonl；向量+meta 成对原子目录替换；不在 meta 的重算。 |
| 38 | 接受并修改 | 索引 signature；阈值/分组/prompt 不触发重建；CLIP 品类单独签名。 |
| 39 | 接受并修改 | 顶部常驻状态行；不写「找到同款」；固定提示「系统不判断同款」。 |
| 40 | 接受并修改 | 验收对子只当测试集；上线后积累 eval/。 |
| 41 | 接受并修改 | 邻居只报告不判定；冒烟以自身第一为准。 |
| 42 | 反驳 | 需求只要打开文件夹；模型文件位置是图库整理问题。 |
| 43 | 接受并修改 | 查询图不落盘；日志只记 hash 与耗时；缓存 30 分钟。 |
| 44 | 接受并修改 | /api/status 报三项健康；每种故障一句中文 + 下一步。 |
| 45 | 接受 | 改口：app.js 重写状态逻辑。 |
| 46 | 接受并修改 | 入口减为 安装.bat / 启动.bat；认图改为网页按钮后台跑。 |
| 47 | 接受并修改 | 状态行常驻待认数，认完归零并热加载。 |
| 48 | 部分接受 | 只合并内容哈希完全相同的重复文件；近似重复不合并（否则越界成分组）。 |
| 49 | 部分接受 | 同上；近似重复挤占前 100 记为已知局限。 |
| 50 | 接受 | 承认最难点；靠变体矩阵 + eval；第二版微调。 |
| 51 | 接受并修改 | 查询 8 视角 TTA。 |
| 52 | 接受 | 由 3、11 解决：原图上裁再缩 448。 |
| 53 | 接受并修改 | query_id；新上传时下拉重置。 |
| 54 | 反驳（被 15 取代） | 分数改为显示。 |
| 55 | 接受并修改 | 核对表改三档；A1/A6/A7/A8/A12 标效果待验。 |

**仍需用户提供**：① 验收前任何 1～2 组对子；② 吊坠/项链是否合并（可到验收时再定）。
