# 使用说明（给设计师本人）

## 第一次（只做一次）

1. 双击 `安装.bat`。它会：建一个独立的 Python 环境、装依赖、先装能用的 CPU 版再试显卡版、下载约 1 GB 模型。看到「安装完成」再关窗口。
   - 国内下载模型慢：在 `.env` 里加一行 `HF_ENDPOINT=https://hf-mirror.com`，重新运行。
2. 双击 `启动.bat`，等窗口出现网址（一般是 http://localhost:8787），用浏览器打开。
3. 页面顶部点「开始认图」。第一次要把 5 万多张图全认一遍，显卡下大约 1～3 小时，CPU 要过夜。可以随时关掉，下次接着认。
4. 顶部显示「已认数量接近总数」就完成了。

## 每天用

1. 双击 `启动.bat`，打开网址。
2. 左边「客人图」选一张图，点「找同款」，等十几秒。
3. 结果列表越上面越像，最多 100 张。
   - 顶上如果是橙色「没找到很像的同款」，下面就是可以改的款。
   - 顶上如果是绿色「最高相似度 0.8x」，说明有比较像的，但是否同款请自己看。
4. 点某张的「打开所在位置」，资源管理器跳到那张图。
5. 品类看错了（比如把戒指当吊坠）：左边下拉选对的品类，点「按这个品类重搜」，不用重新选图。

## 补了新图之后

1. 顶部「扫描图库」（路径填新图所在的根目录，或者原来的目录）。
2. 顶部会显示「待认 N 张」，点「认新图」。只认新图，几分钟到几小时。
3. 认完自动能搜。

## 验收前

- 把客人图和库里对应的那张放进 `eval/pairs.json`（格式见 `search_service/eval.py` 开头），运行 `npm run eval` 看名次。
- 先确认目标图在库里：`py -3.11 -m search_service.check_in_library 文件名`

## 出问题时看这里

| 页面提示 | 怎么办 |
|---|---|
| 检索内核没有启动 | 看 `启动.bat` 窗口里最后几行红字；多数是没跑 `安装.bat` 或端口 8788 被占 |
| 检索内核正在启动 | 等 10 秒左右 |
| 还没有认过图 | 点「开始认图」 |
| 视觉 API 未成功 | `.env` 里的 API 不通；系统会用本地粗判品类，认错就手动选 |
| 没看出这是什么品类 | 手动选品类再重搜 |
| 这张图打不开 | 另存为 JPG 再试 |
| 文件已不在原位置 | 图被移走了；重新「扫描图库」再「认新图」，认图会认出是同一张图不重算 |
