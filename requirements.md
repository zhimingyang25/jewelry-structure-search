# 珠宝款式比对检索 — 需求基线

本文只记录访谈中用户已明确确认的信息。
未确认的内容单独列出。AI 的推测与建议单独列出，不视为已确认需求。

访谈日期：2026-09-16
来源：用户对现有项目 `jewelry-structure-search` 的说明，以及后续需求访谈（grill-me）。

---

## 1. 项目目标

帮助珠宝设计师把**客人给的成品图**（偶尔也包括设计师自己画的草图）和**本地电脑里已有的珠宝设计图**（几乎都是犀牛 / JCAD 渲出来的图）放在一起比对，找出：

1. **同款**：就是做过的那一款，改一改就能交差；
2. **类似、可以改**：不是同款，但大方向对，大约花半天能改成客人要的。

即使两边外观差很多（渲染 vs 精修、光线、背景、成色不同），只要款式结构是同一款或很接近，也要能对上。

系统要找的是**那一个具体款式**，不是「是不是戒指」「是不是群镶」这类粗分类。

---

## 2. 用户

- 第一版只给**设计师本人**在自己电脑上用。
- 以后工作室其他人可能要用，第一版不按多人同时使用来要求。

---

## 3. 已确认需求

### 3.1 日常怎么用

- **主流程（每天都有）**：客人拿一张成品图过来（精修图、实拍图都行），在本地图库里找同款或能改的款。
- **次要流程（比较少）**：设计师自己随手画一张草图，也在本地图库里找最接近的款。
- 第一版以客人成品图为准；草图也要能搜，不要完全不能用，但**不要求**和成品图一样准。

### 3.2 什么叫「同款」

同款卡得很紧，必须同时几乎一样：

- 戒头（或对应部位）长什么样；
- 主石什么形；
- 戒臂（或对应结构）怎么走；
- 石头怎么镶。

只要光环圈数不一样、戒臂分叉方式不一样、头的结构不一样，哪怕一眼都像「光环戒指」，也只能算「类似、可以改」，不能算同款。

**成色、石色可以换**：白金、黄 K、玫瑰金，或主石换了颜色，只要款式结构一样，仍是同款。成色和石色只是换料，不是换款。系统不应因为颜色不一样就把它们拆成两个款。

### 3.3 什么叫「类似、可以改」

- 「可以改」大约是**半天**的工作量：结构还在，但要认真改一版。不是改几刀就能交差，也不是从零重做。
- 「类似」也要大方向对。例如客人图是「圆钻 + 一圈碎钻围着 + 戒臂分开」，能改的也应还是光环、分叉戒臂这类，只是圈数、爪数、宽窄差一点。
- **完全另一种头**（例如一大朵群镶花头）不要排太前面。
- 主石形状不同仍可算能改：客人图是圆钻、库里是椭圆钻，但光环、分叉戒臂等大结构还在，可以出现在后面那批「类似、可以改」里。

### 3.4 结果怎么呈现

- 按**一张一张图**列出来，越往上越像。
- 同一款若有正面、侧面、戴手上等多张图，可以连续出现；**不要求**先收成一组再点开。
- **不要**在每张图上标注「同款 / 类似」。用户自己看图判断。
- 一次最多大约 **100 张**。用户更怕漏掉，愿意往后翻。
- 认出一张图之后：打开这张图所在的文件夹。渲染图和犀牛 / JCAD 文件一般在同一个文件夹里。系统只要打开文件夹，不必直接打开模型文件。

### 3.5 品类与搜索范围

- 用户**不用先选文件夹**。客人图丢进去，在全部图库里找。
- 系统自己判断客人图是戒指、吊坠还是耳环等，然后**只出对的那一类**。
- 客人图是戒指，结果里就只出戒指。吊坠、耳环哪怕长得很像也不要混进来（品类不对，改不成）。
- 若系统把品类认错：用户愿意自己再选一次「这是戒指」（或对应品类），重新搜一遍。不为了防止看错，就让其它品类混进结果。

### 3.6 客人图画面

- 两种都常见：有的很干净（一件首饰、白底或背景简单）；有的很满（戴在手上/脖子上，旁边还有别的首饰，背景杂）。
- 两边都要能用，不能只认干净白底图。
- 画面里有好几件东西时：看**最显眼、最大的那一件**。第一版不要求用户框选，也不要求把每一件都分开找。

### 3.7 库里没有同款时

- 不要把上面几张「有点像」的图装成同款。
- 先明白告诉用户：**没找到很像的同款**。
- 然后再给一批「类似、可以改」的图，用户自己挑。
- 列表仍是一张一张往下排，不用每张图贴标签；只是在「根本没有同款」时，上面要有一句实话。

### 3.8 等待时间与图库更新

- 丢进客人图后，等**十几秒到半分钟**可以接受，准比快更重要。
- 图库第一次「认一遍」，电脑开着转一下午、甚至过夜可以接受。
- 图库不是天天涨，而是过一段时间才补一批新渲染图。补进去之后，电脑再转几小时、甚至晚上开着认一遍，之后能搜到即可。**不要求当天立刻能搜到。**

### 3.9 图是否可以传到网上

- 传到网上的 AI **可以接受**，找得准、用着方便更重要。不是必须关在这台电脑里。

### 3.10 图库内容

- 这几万张**几乎都是用户自己犀牛 / JCAD 渲出来的图**。实拍、网图很少。
- 现有项目已扫描约数万张，分布在多个本地文件夹中（见现有 `data/images.json`）。用户口头还提过更大规模的图库，是否还有未扫描的图见「尚未确定」。

### 3.11 界面

- 现有网页用户并不执着。只要能把同款、接近的款找出来，界面可以重做，不必沿用现在这一套。

### 3.12 用来检查准不准的例子

- 用户手头有「铁定同款」的例子：客人成品图对得上库里某张渲染图。
- 例子目前散落在聊天记录、桌面等处，不一定马上整理成文件夹。
- 真正试系统时，用户能当场翻出大约 **3 到 5 组**（客人图 + 库里对应那张），边试边找，不用提前整理。

---

## 4. 功能范围（第一版要做）

1. 用户上传一张客人成品图（或一张草图）。
2. 系统在全部本地图库中检索。
3. 系统判断客人图的品类，并只返回该品类的图。
4. 返回一条按相似度从高到低排列的图片列表，最多约 100 张。
5. 若判断库里没有很像的同款，在列表上方明确说明「没找到很像的同款」，再给出类似、可改的图。
6. 用户可手动指定品类后重新搜索（品类判断错了时）。
7. 用户点击某张结果，打开该图所在的文件夹。
8. 图库需要先做一次「认一遍」；之后过一段时间补新图，再认一遍新图即可被搜到。

---

## 5. 不做什么（第一版明确不做）

以下均来自用户已确认的选择，不是额外发挥：

- 不直接打开犀牛 / JCAD 模型文件，只打开图片所在文件夹。
- 不把同一款的多张图收成一组再点开。
- 不要求用户先选文件夹再搜（默认全库搜）。
- 不把画面里每一件首饰都分开检索；只看最大、最显眼的那一件。
- 不要求草图检索达到和成品图同样的准确度。
- 不按「工作室多人同时使用」来做。
- 不在每张结果图上标注「同款 / 类似」。
- 不为了「防看错品类」而把其它品类混进当前品类的结果里。
- 现有「给每张图打结构词、再用词去对」的方式，不能再作为找回同款的主手段。（用户已确认：真正要的是同款比对，不是同类标签匹配。）

---

## 6. 业务规则

| 规则 | 说明 |
|------|------|
| 同款判定 | 头、石形、戒臂/结构走向、镶法几乎一样才算同款。 |
| 不算同款 | 光环圈数不同、分叉方式不同、头的结构不同 → 最多算类似。 |
| 颜色 | 金属成色、主石颜色不同，结构一样仍算同款。 |
| 类似可改 | 大方向相同（如仍是光环+分叉），细节可差；改大约半天。 |
| 类似可改（主石） | 圆钻 vs 椭圆钻，大结构仍在 → 可出现在后面的类似结果中。 |
| 类似不可靠前 | 完全另一种头不要排太前。 |
| 品类隔离 | 客人图是哪一类，结果只出那一类。 |
| 品类纠错 | 认错后由用户手动指定品类再搜，不自动混入其它品类。 |
| 无同款 | 必须先说明没找到很像的同款，再给类似结果；不得把「有点像」装成同款。 |
| 多物体画面 | 以最大、最显眼的一件为准。 |
| 结果数量 | 最多约 100 张，按像的程度从高到低。 |

---

## 7. 输入与输出

### 输入

- 一张客人成品图（干净或较满都有），或一张设计师手绘草图。
- 本地已扫描的珠宝设计图库（几乎均为渲染图）。
- 可选：用户手动指定品类（品类判断错误时）。

### 输出

- 一条图片列表，越靠前越像，最多约 100 张。
- 每张可打开其所在文件夹。
- 当系统判断没有很像的同款时：列表上方的一句明确说明 + 随后的类似结果。

---

## 8. 异常情况

| 情况 | 已确认的处理 |
|------|----------------|
| 客人图很满、有多件首饰 | 按最大、最显眼的一件搜。 |
| 品类判断错误 | 用户手动指定品类后重新搜索。 |
| 库里没有同款 | 明确告知「没找到很像的同款」，再给类似、可改的图。 |
| 草图 | 第一版要能搜，但不保证与成品图同等准确。 |
| 图库尚未完成第一次「认一遍」 | 用户接受先花一下午或过夜完成后再日常使用。 |
| 新图刚放进文件夹 | 不要求当天能搜到；再认几小时后能搜到即可。 |

---

## 9. 边界条件

- 第一版用户为单人、本机使用。
- 等待单次查询约十几秒到半分钟可接受。
- 第一次处理全库可过夜。
- 结果上限约 100 张。
- 开始天天用的心理标准：铁定同款应出现在大约前 20 张（见验收标准）。漏掉比排得靠后更不能接受，但「可以天天用」仍要求比较靠前。
- 现有项目已扫描约数万张；用户曾提到可能还有未扫描图片。第一版以当前已扫描图库为检索范围，除非用户之后明确要求纳入未扫描路径（见尚未确定）。

---

## 10. 验收标准（尽量可测试）

用用户当场提供的大约 **3 到 5 组「铁定同款」** 进行验收。每一组包含：客人成品图 + 库里对应的那张（或那几张）渲染图。用户确认这些组在测试时能翻出来。

记：每组的「目标图」= 用户指出的库里那张对应渲染图。

| 编号 | 标准 | 如何判定通过 |
|------|------|----------------|
| A1 | 同款不漏且够靠前 | 在 3～5 组铁定同款中，**每一组**把客人图作为查询时，目标图出现在结果列表的**大约前 20 张**内。 |
| A2 | 结果不超过约定数量 | 单次查询返回的图片数量 ≤ 约 100 张。 |
| A3 | 排序方向 | 列表按「越往上越像」排列（不要求自动标注同款/类似）。 |
| A4 | 品类隔离 | 用明确为戒指的客人图查询时，结果中不应出现吊坠、耳环等其它品类。其它品类同理。 |
| A5 | 品类可纠正 | 若系统把戒指误判为其它品类：用户手动指定「戒指」后重新搜索，目标戒指图应能按 A1 被找到，且结果仍只含戒指。 |
| A6 | 无同款时的说明 | 选用用户确认「库里没有同款」的客人图查询时，界面须先明确提示「没找到很像的同款」，然后再给出类似结果；不得把最前面几张表现为同款。 |
| A7 | 类似结果的方向 | 在 A6 或同款很少的情况下，排在前面的「类似」图应仍属同一大方向（例如仍是光环+分叉类，而不是完全另一种头顶在最前）。主石圆/椭圆不同的图可以出现在后面。 |
| A8 | 颜色不拆款 | 若某组铁定同款仅成色或石色不同、结构相同：目标图仍应能按 A1 被找到，不得仅因颜色不同而找不到或被排除。 |
| A9 | 多角度可以多张出现 | 同一款若库里有多张不同角度，允许它们作为多条结果连续出现，不强制合并为一组。 |
| A10 | 打开文件夹 | 点击任一结果，应打开该图片所在的文件夹（Windows 资源管理器中能看到该文件）。 |
| A11 | 全库搜索 | 用户无需先选择子文件夹即可搜索已纳入的全部图库。 |
| A12 | 忙碌画面 | 用一张画面较满、主体仍是一件最大首饰的客人图查询时，系统按该主体检索，且仍满足 A4。 |
| A13 | 等待可接受 | 单次查询从提交客人图到出列表，大约在十几秒到半分钟量级（第一版按这个预期验收，不要求「点一下就出」）。 |
| A14 | 草图最低限度 | 用一张用户自己的草图查询时，系统能返回结果列表且不报错；不要求草图也满足 A1。 |
| A15 | 新图延迟可接受 | 新放入图库的图，不要求当天可搜到；在「再认一遍」（几小时量级）之后应能被搜到。此项可在第一版上线后用一小批新图单独验证。 |

**开始天天使用的门槛：** A1、A4、A6、A10 必须通过。A1 对当场提供的 3～5 组应整体成立（用户原话：真正的那张要大概排在前 20 张，否则还是不好天天用）。

---

## 11. AI 推测 / 建议（不是已确认需求）

以下是访谈过程中 AI 的判断，**用户未把它们写成必须实现的产品规则**。实现时可以作为参考，不能当成用户已经点头的需求。

1. 最难的是客人精修成品图和犀牛渲染图外观差很大，但款是同一个（跨域）。
2. 现有项目用结构标签对词，所以经常只能找到「都是戒指」，找不到「那一款」。
3. 用户接受把图传到网上的 AI，是为了找得准，不是特别喜欢把图传到外面。
4. 建议第一版用「画面像不像」来排序，品类判断只做过滤；库里没有很像的同款时，再用一句话说明，而不是给每张图打同款/类似标签。
5. 建议第一版不要再把「打标签 + 对词」当作找回同款的主搜索。

---

## 12. 尚未确定的问题

1. **图库完整范围**：用户曾提到本地图库可能更大；现有项目已扫描约数万张，分布在多个本地目录。其余图是否在别的硬盘/文件夹、第一版要不要一起搜，未确定。
2. **草图「凑合能用」的具体准度**：只确定要能搜、不要求与成品图同等准确；没有可测试的草图命中名次。
3. **铁定同款样本的整理**：测试时能当场翻 3～5 组，但现在不整理成固定文件夹；验收当天才能固定样本清单。
4. **「没找到很像的同款」如何判定的内部标准**：用户要求有这句话，但没有规定系统用什么阈值才说这句话（实现时需另定，且不能冒充已确认需求）。
5. 本文**不规定**具体技术栈、数据库、框架或代码实现方式。用户未指定这些。

---

## 13. 明确区分

| 类型 | 内容 |
|------|------|
| 用户明确说过 | 第 1～10 节、第 5 节「不做什么」、第 3 节全部条目。 |
| AI 推测 / 建议 | 第 11 节。 |
| 尚未确定 | 第 12 节。 |
